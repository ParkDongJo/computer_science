package data_algorithm_test.common

import kotlin.math.*

fun main(args: Array<String>) {
    var a = 7
    var b = 8
    var maxNum = max(a,b)
    var minNum = min(a,b)
    var absNum = abs(a-b)

    var n = 10
    var sqrtNum = sqrt(n.toDouble()).toInt()

    var powNum = 2.0.pow(2)
}